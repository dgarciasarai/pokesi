{
  "name": "pokesi",
  "version": "1.0.0",
  "description": "POKESI, the perfect bowl",
  "scripts": {
    "lint:css": "stylelint client/**/*.css client/**/*.html",
    "lint:js": "prettier-standard client/**/*.js server/**/*.js",
    "start:client": "cd client && parcel --https index.html",
    "start:server": "cd server && json-server data.json"
  },
  "lint-staged": {
    "linters": {
      "client/**/*.css": [
        "npm run lint:css",
        "git add"
      ],
      "client/**/*.js": [
        "npm run lint:js",
        "git add"
      ],
      "server/**/*.js": [
        "npm run lint:js",
        "git add"
      ]
    }
  },
  "contributors": [
    {
      "name": "Pablo Molina"
    },
    {
      "name": "Sarai Díaz"
    }
  ],
  "license": "ISC",
  "dependencies": {
    "babel-polyfill": "^6.26.0",
    "husky": "^1.1.2",
    "hyperapp": "^1.2.9",
    "json-server": "^0.14.0",
    "lint-staged": "^7.3.0",
    "parcel-bundler": "^1.10.3",
    "prettier-standard": "^8.0.1",
    "stylelint": "^9.6.0",
    "stylelint-config-standard": "^18.2.0"
  }
}
